<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jhta.groupware.mybatis.work.RequestWorkMapper">
	<insert id="insertWorkStart" parameterType="map">
		insert into worklist (wlist_num,wlist_start,wlist_type,emp_num)
		values(seq_worklist.nextval , to_date(#{wlist_start},'YYYY/MM/DD hh24:mi') , #{wlist_type} ,#{emp_num})
	</insert>
	<insert id="insertWorkEnd">
	
	</insert>
	<!-- 
	<select id="selectWorkList" resultType="worklist" parameterType="map">
		select * from worklist where emp_num=#{emp_num} and wlist_start=to_date(#{wlist_start},'YYYY/MM/DD')
	</select>
	 -->
	<select id="selectWorkStart">
	
	</select>
	<select id="selectWorkEnd">
	
	</select>
</mapper>